Create a visual map of the structure of the database (tasks, modules, and connections). When one opens the visual map, you show modules that are connected. A module is connected with another one if one of it's task has an output file that is used as an input file for a task of the other one, or vice cersa. Clicking on one module, it opens the tasks of the module as nodes and how they are connected to each other. A task is connected to another task if an output file is used as an input file of another task or vice versa. I think using d3 javascript library would be ok. Maybe when rebuilding the database, create the d3 file in the database folder. I want it to be in a different file from the model flow database. Maybe name it menaningfully, like d3_map.json or js (the extension i do not know). Then the webgui can use it to show in a new window the map. The map should be interactive, allowing users to click on modules and tasks to view their details and connections. Additionally, provide options to filter the view based on specific criteria, such as task status or file types. What other feature do you think would be useful?

In the webgui, can i also have a button to open the log of the flask server?